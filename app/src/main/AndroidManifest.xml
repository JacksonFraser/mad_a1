<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.s3529589.mad_a1">

    // Manifest permission for reading contacts
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"

        >
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.FriendMenuActivity"></activity>
        <activity android:name=".Activity.MeetingMenuActivity"></activity>
        <activity android:name=".Activity.DatePickerActivity"></activity>
        <activity android:name=".Activity.DisplayFriendActivity"></activity>
        <activity android:name=".Activity.CreateFriendActivity"></activity>
        <activity android:name=".Activity.ScheduleMeetingActivity"></activity>

    </application>

</manifest>